// due to constraints in original proble, use long long int everywhere

#include<bits/stdc++.h>
using namespace std;

int main(){

    int n, q; cin>>n>>q;

    vector<int> cost(n), saltiness(n);
    for(int i=0; i<n; i++) cin>>cost[i];
    for(int i=0; i<n; i++) cin>>saltiness[i];

    for(int i=0; i<q; i++){

        int t; cin>>t;
        if(t==0){

        }else{



        }

    }

}

